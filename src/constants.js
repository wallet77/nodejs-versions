const constants = {
  cacheDir: './cache/',
  changelogDir: 'changelogs/',
  samplesDir: 'samples/',
  TTL: 86400, // one day,
  timeout: 5000, // max time to request public url
  urlReleases: 'https://nodejs.org/dist/index.json'
}

module.exports = constants
